{
  "name": "gatekeepeer",
  "version": "0.1.0",
  "description": "Telegram-бот авто-приёма заявок на вступление в приватные каналы/чаты",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bunx wrangler dev",
    "deploy": "bunx wrangler deploy",
    "tail": "bunx wrangler tail",
    "setup": "bun scripts/setup.ts",
    "migrate": "bun scripts/migrate.ts",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@libsql/client": "^0.17.3",
    "grammy": "^1.31.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
